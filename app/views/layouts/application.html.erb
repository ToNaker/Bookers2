<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bookers2" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <!-- favicon -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <!-- Font Awesome -->
    <link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- Rails assets -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <!-- Header -->
    <%= render "layouts/header" %>

    <!-- Flash messages -->
    <div class="mt-2 ms-3">
      <% if notice.present? %>
        <p class="text-dark m-0"><%= notice %></p>
      <% end %>

      <% if alert.present? %>
        <p class="text-dark m-0"><%= alert %></p>
      <% end %>
    </div>

    <!-- Main content -->
    <main class="container mt-3 flex-grow-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="mt-3 py-3 text-center">
      <div class="container">
        <small class="text-muted">CopyRight Infratop.inc</small>
      </div>
    </footer>

    <!-- Bootstrap JS（≡ を動かすため必須） -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
